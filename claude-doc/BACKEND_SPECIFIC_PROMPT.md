**Prompt: Senior-Grade Codebase Architecture & Rewrite**

I have two projects in my root directory. **Natura Care** is the primary reference project.

Using Natura Care as the **structural and functional blueprint**, I need you to produce a **fully rewritten, production-ready codebase** that:

- **Mirrors the features and purpose** of Natura Care, but treats it as a reference — not a copy
- Is architected by a **senior software engineer's standard**: clean separation of concerns, SOLID principles, DRY, KISS, and YAGNI applied throughout
- Follows **industry best practices** for the tech stack in use (folder structure, naming conventions, error handling, logging, validation, security, performance)
- Is **scalable** — designed to handle growth in users, features, and team size without requiring structural overhaul
- Is **reusable** — shared logic abstracted into utilities, hooks, services, or modules; no duplicated code
- Is **optimized** — efficient rendering/querying, lazy loading where applicable, minimal bundle/payload size, and no unnecessary computations
- Has a **robust architecture** — clear data flow, predictable state management, well-defined API boundaries, and graceful error handling at every layer
- Is **maintainable** — self-documenting code, consistent patterns, and structure that a new developer can onboard into quickly

> Treat the output as if it will be reviewed in a senior engineering code review and shipped to a production environment with real users.

